# Job Api JWT implementation

it is a simple app that uses JWT to authenticate user. This app only focuses on access tokens and not in refresh tokens. Once access token is expired, user needs to login again.
